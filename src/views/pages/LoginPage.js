import React from 'react';
import LoginView from '../components/Auth/LoginView'; // Corrected import path

const LoginPage = () => {
  return <LoginView />; // Render LoginView component
};

export default LoginPage;